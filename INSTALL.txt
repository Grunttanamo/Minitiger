MINITIGER WEB – PHASE 18.5.1
Plugin Release ZIP Hotfix

VORAUSSETZUNG
- Branch: minitiger-v12.1
- Phase 18.5.0 ist eingespielt.
- `Build Minitiger Virtual Sync Plugin` war grün.
- Der erste Release-Versuch `1.0.3.0` ist vor der GitHub-Release-Erstellung beim MD5-Schritt fehlgeschlagen.

INSTALLATION AUF DEM RASPBERRY PI

1) Repository öffnen:
   cd ~/minitiger-web

2) Patch entpacken:
   unzip -o ~/minitiger_native_phase18_5_1_plugin_release_zip_hotfix.zip -d ~/minitiger-web

3) Prüfen:
   chmod +x VERIFY_PATCH.sh
   ./VERIFY_PATCH.sh

4) Commit + Push:
   git add .github/workflows/minitiger-plugin-release.yml \
     INSTALL.txt CHANGELOG_Minitiger_Web.txt files.txt VERIFY_PATCH.sh \
     Minitiger_Gedaechtnis

   git commit -m "Fix Minitiger plugin release archive"
   git push

ERNEUTER RELEASE-TEST
GitHub -> Minitiger -> Actions -> Release Minitiger Virtual Sync Plugin
-> Run workflow

Branch:
  minitiger-v12.1

Version:
  1.0.3.0

Changelog:
  Initial public Minitiger plugin repository release for Jellyfin 12.1.

WICHTIG
- Derselbe Versionswert `1.0.3.0` kann erneut verwendet werden, weil der fehlgeschlagene Lauf den Schritt `Create GitHub release` nie erreicht hat.
- Wenn der Workflow grün wird, anschließend prüfen:
  1. GitHub Release `plugin-v1.0.3.0` existiert.
  2. Release enthält `Minitiger.VirtualSync_1.0.3.0.zip`.
  3. `plugin-repository/manifest.json` enthält Version `1.0.3.0`.
  4. Plugin im Jellyfin Catalog sichtbar/installierbar.
